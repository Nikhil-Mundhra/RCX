const express = require('express');
const router = express.Router();

// Placeholder - main routes are in server.js for scaffold
router.get('/', (req, res) => res.status(200).json({ message: 'Use /api/properties on server root' }));

module.exports = router;
